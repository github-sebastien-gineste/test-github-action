name: Ajouter un commentaire à la Pull Request en TS

on:
  pull_request:
    types: 
      - opened
      - edited

jobs:
  add_comment_ts:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout le code
      uses: actions/checkout@v3
    - name: Use node.js 16.x
      uses: actions/setup-node@v2
      with:
        node-version: 16.x
        cache: npm
        cache-dependency-path: package-lock.json
    - name: Install dependencies
      run: cd ./.github/ts/ && npm ci
    - name: build
      runs: cd ./.github/ts/ && npm run build
    - name: run action
      runs: node ./.github/ts/dist/src/index.js

        
        